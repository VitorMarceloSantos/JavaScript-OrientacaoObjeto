/* Void: é o oposto de Any, geralmente declarado em funções, pois em variáveis não tem muita utiliade, já que aceita apenas undefined.*/
function logMessage() {
    console.log("Mensagem do Sistema.");
}
logMessage();
